apiVersion: apps/v1
kind: Deployment
metadata:
  name: neurops-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: neurops-frontend
  template:
    metadata:
      labels:
        app: neurops-frontend
    spec:
      containers:
      - name: frontend
        image: neurops-frontend:latest
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: neurops-frontend-service
spec:
  selector:
    app: neurops-frontend
  ports:
  - port: 3000
    targetPort: 3000
  type: LoadBalancer